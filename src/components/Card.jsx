import Header from "./Header"


let Card = ({ date , logo , btn_text , heading , footer_text }) => {
    return (<div>
        <Header date={date } logo={logo} />
    
    </div>)
}

export default Card